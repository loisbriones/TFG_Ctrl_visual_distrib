/**:
  ros__parameters:
    debug: False
    camera:
      mode: 2
    detection:
      target_color_1: "verde"
      target_color_2: "azul"
      sector_color: "naranja"
      kernel_size: 5
      min_area: 70 
      
    # Parámetros para el controlador
    controller:
      car_name: "rbi_car_01"
      finish_debounce: 1.0

    # Parámetros para el Arduino
    arduino:
      port: "/dev/ttyACM0"
      rail_id: 2
      baudrate: 115200
      calib_speed: 55

